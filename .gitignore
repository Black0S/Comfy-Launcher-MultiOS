# ComfyUI install (repo + venv, created by the scripts)
comfyui/

# Self-signed TLS certificate generated by comfy.sh — NEVER commit the private key
certs/

# Saved launch profiles (may contain private addresses, e.g. Tailscale IPs)
profiles.conf

# macOS
.DS_Store
